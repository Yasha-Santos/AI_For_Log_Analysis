This folder is created for the t5 model. 
the model itself is too large to upload to github so in this folder you will 
find some data to train the model and the code to train and create the model. 
The naming of the folders is wonky as github is being a little weird, this will 
be fixed. But use the data and python file to create the model and move it to the partent
folder with the name 't5-log-parser'(I know its this folders name, this will be fixed)
as per the 'model_path' definition in 'run_model_on_file.py' file. Alternatively
name it whatever and modify the definition. 
