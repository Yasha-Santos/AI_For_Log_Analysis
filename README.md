# Jarvis Log Anomaly Detection — Final App

```bash
cd final_app
pip install -r requirements.txt
cd "jarvis_dashboard\Dashboard-For-AI - Copy\log_analyzer"
python manage.py runserver
```

Open http://127.0.0.1:8000/
